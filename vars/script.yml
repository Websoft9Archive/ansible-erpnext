docker_applications: 
  - phpmyadmin

# init passsword of database
init_db: 
  mariadb:
    admin: root
    password: "123456"  
  
# init password of applications
init_application:
  erpnext:
    username: Administrator
    password: admin
    commands : 
      - sudo -H -u frappe bash -c "cd /home/frappe/frappe-bench && export GIT_PYTHON_REFRESH=quiet && bench --site {{erpnext_site_name}} set-admin-password $new_password"
      

    

